# Workflow Activation

Do not activate a workflow until:

- credentials are stored in n8n credential vault;
- webhooks are protected;
- platform callback signing is tested;
- dry-run output is accepted;
- owner approves activation;
- workflow version is recorded;
- rollback path is documented.

Publishing workflows additionally require Meta permissions, valid long-lived tokens, public media hosting, duplicate protection, and manual publication approval.

